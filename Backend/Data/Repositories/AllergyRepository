using System;
using System.Collections.Generic;
using System.Linq;
using Data;
using Microsoft.EntityFrameworkCore;

namespace Data
{
   public class AllergyRepository : Repository<AllergyRepository>, IAllergyRepository
   {
       public AllergyRepository(CloudCureDbContext context) : base(context)
        {
        }

        public IEnumerable<AllergyRepository> SearchByPatientId(int query)
        {
            throw new NotImplementedException();
        }
    }
}